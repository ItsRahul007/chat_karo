class Message {
  final String message;
  // final String sender;
  final bool isMe;
  final String time;

  Message({
    required this.message,
    // required this.sender,
    required this.isMe,
    required this.time,
  });
}
